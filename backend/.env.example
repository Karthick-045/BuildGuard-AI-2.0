# MySQL with password:
# DATABASE_URL=mysql+pymysql://root:YOUR_PASSWORD@localhost:3306/buildguard

# MySQL without password:
DATABASE_URL=mysql+pymysql://root@localhost:3306/buildguard

# SQLite local fallback (if MySQL is not installed):
# DATABASE_URL=sqlite:///./buildguard.db

UPLOAD_DIR=uploads
CORS_ORIGIN=http://localhost:5173
PORT=8000
HOST=0.0.0.0
